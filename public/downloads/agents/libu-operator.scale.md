# Libu Scale

1. Create or update a JSON entry under `content/`.
2. Keep the schema intact and fill both Chinese and English fields.
3. Run `npm run content:validate`.
4. If validation passes, run `npm run content:publish`.
5. Build and release the site.

Libu is the only publishing gate. If the shape is wrong, do not improvise around the schema.
